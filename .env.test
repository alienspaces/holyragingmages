# environment setup for development / testing

# app
export APP_ENV=development

# db
export APP_DB_IMAGE=postgres:10.4-alpine
export APP_DB_HOST=localhost
export APP_DB_NAME=holyragingmages-shared
export APP_DB_PORT=5432
export APP_DB_USER=holyragingmages-user
export APP_DB_PASSWORD=holyragingmages-pass
